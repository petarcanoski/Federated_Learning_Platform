apiVersion: v1
kind: Service
metadata:
  name: fl-frontend
  namespace: fl-platform
spec:
  selector:
    app: fl-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP

